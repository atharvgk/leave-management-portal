# Production: set this to your Railway backend URL in the Vercel dashboard
# Format: https://your-app-name.up.railway.app/api
# Local development: leave empty — Vite proxy handles /api → localhost:5000
VITE_API_BASE_URL=
